"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var tslib_1 = require("tslib");
var shortid_1 = tslib_1.__importDefault(require("shortid"));
exports.generateShortId = function (length) {
    length = length ? length : 10;
    if (!Number.isInteger(length) || length < 5 || length > 16) {
        return "";
    }
    var id = "" + shortid_1.default.generate() + shortid_1.default.generate();
    if (length > 10) {
        id = "" + id + shortid_1.default.generate();
    }
    id = id.replace(/-|_/g, "");
    return id.slice(0, length);
};
